# RiskSense AI — project status

As of: 2026-09-15
Release classification: repository implementation against an unsigned Draft v1 baseline; not externally accepted and not production-ready

## Authority

The requirements in docs/requirements are Draft v1 and have not been signed off. They contain duplicate or ambiguous identifiers and interpretation gaps. Repository-complete means code and automated checks exist at the stated scope. It does not mean the owner/client, TAC, security, legal, operations, or end users have accepted the release.

## Implemented repository capabilities

- Express/TypeScript/Mongoose API with Zod validation, OpenAPI generation, Firebase token verification, application sessions/OTP, tenant-aware RBAC, request limits, structured logging, and health endpoints.
- Copy-on-write personas, scenarios, scoring matrices, and rules; audited active/retired questions; and sequenced reviewed dataset bundles with approval/activation workflows.
- Transactional dataset activation and pinned content/rule/prompt versions.
- JSON assessment turns with persona/scenario selection, deterministic branching, structured fact extraction/clarification, required-fact gates, deterministic rules/scoring, and explanation.
- Decision invariant, accept/override/escalate, review queues, dashboards, lifecycle reconstruction, append-only application audit/hash verification, masking/audited unmasking, aggregate reports/exports, retention, and persistent conformance.
- Figma-informed Requestor, administrator/content, system-administrator, and auditor frontend workspaces, including the bounded System Audit archive interface.
- Generated typed frontend API client, exact 1,114/1,114 English/Bengali message/ICU parity, and 31/31 mocked browser correctness cases.

## Task reconciliation

- T-027 structured content editors and the Figma-informed role-workspace refresh are repository-complete; current lint, typecheck, the 27/27-route Next.js 16 production build and the 31-case mocked Playwright suite are green.
- T-028 manual user/persona/department mapping exists; real SSO claim-to-tenant/role mapping remains open.
- T-037 is deferred and superseded by the JSON request/response design.
- T-038 chat UI/resume exists; SSE/EventSource streaming was removed.
- T-064 persistent conformance runs and flags are implemented.
- T-090 English/Bengali repository coverage is repository-complete with exact 1,114/1,114 key and ICU-variable parity and green aggregate frontend validation. Human Bengali/accessibility/usability acceptance remains open.
- T-100 repository browser coverage is complete at the mocked-API scope (31/31 current cases); production-like full-stack evidence and human UAT remain external gates.
- NFR-08 remains partial because sector choices are hardcoded.

## AI boundary

The product has four internal structured AI stages: persona inference, scenario selection, fact extraction, and explanation. Deterministic code owns scoring/classification. The opt-in test:ai-live command makes one potentially billable synthetic persona inference. It is connectivity/schema evidence only, not validation of five prompt types or production accuracy.

## Operations boundary

- The optional scheduler is one in-process daily owner for retention, audit verification, and conformance. There is no Redis/BullMQ/Agenda queue or report-cache job.
- Assessment retention currently enforces closed-assessment handling. Evidence/dataset-history/per-object enforcement and external audit cold storage remain open.
- Audit archive export is bounded and manifested, but external immutable custody/retrieval is not configured by the repository.
- DR status stores operator-attested timestamps/outcome/reference. It does not query Atlas or prove backup/PITR.
- Sensitive values, result messages and reconstruction answer/fact values are masked in responses with audited privileged unmasking; there is no field-encryption middleware.
- There is no controller layer, SSE/EventSource contract, scenario factSchema, or second audit database in the implementation.

## External gates before release

1. Signed requirements and resolved interpretations.
2. TAC-approved production content, mappings, default dataset, rules/matrices, and goldens.
3. Real Firebase/Admin configuration, authorized domains, email-verification template/delivery, production OTP provider, and verified signup/sign-in UAT.
4. IdP/SSO claims for tenant/role mapping and lifecycle.
5. Approved hosting/regions, TLS/domain, secrets, current dependency disposition, and penetration/security acceptance.
6. Monitoring, alerts, log custody, SLOs, on-call, and representative scale/capacity evidence.
7. Atlas backup/PITR and isolated restore/failover evidence with accepted RTO/RPO.
8. External immutable audit archive/cold storage, retrieval, and approved per-object retention/legal hold.
9. Representative live-model validation and accepted accuracy evidence.
10. Role UAT, accessibility, Bengali review, and comprehension/usability sign-off.
11. Privacy, legal, residency, provider-processing, and sector/control approvals.
12. Owner visual acceptance of the Figma-informed implementation, requirement-led extensions for incomplete frames, and final Figma comment dispositions.

## Verification commands

Backend, from risk-sense-ai-backend:

    npm ci
    npm run lint
    npm run typecheck
    npm test
    npm run openapi
    git diff --exit-code -- openapi.json
    npm run build

Frontend, from risk-sense-ai:

    npm ci
    npm run lint
    npm run typecheck
    npm run e2e:frontend
    npm run build

Database/content, only against the explicitly intended environment, includes content:verify, accuracy, audit:verify, schema:scan, retention dry-run, and guarded rule-migration dry-run. The one-call live AI smoke is explicit, billable, and excluded from ordinary CI.

See docs/ai/RequirementsCoverage.md, docs/ai/KnownIssues.md, tasks/sprint-release-readiness.md, and docs/UAT.md for the current evidence boundary.

No production deployment, external acceptance, owner Figma acceptance/comment completion, release tag, or push is asserted by this status file.
